# audiostream_converter
this script will convert any audiostream (AAC, DTS, ...) of a .mkv file to EAC3 764k with the same channels (via ffmpeg), then remux these audiostreams to a new .mkv (via mkvmerge)
